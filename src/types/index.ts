export type Trend = {
  id: string;
  comment: string;
  createdAt: string;
  updatedAt: string;
};

export type OGP = {
  href: string;
  title: string;
  description: string;
  src: string;
  createdAt: string;
  updatedAt: string;
};

export type Tag = {
  title: string;
  color: string;
  textColor: string;
  createdAt: string;
  updatedAt: string;
};
